[project]
name = "cc.statusq-cpu"
version = "0.8.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "core-log-engine>=0.2.2",
    "gitchangelog>=3.0.4",
    "psutil>=7.2.2",
    "pytest>=9.0.2",
    "ruff>=0.15.2",
]
